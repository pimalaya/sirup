# socks-dir = "/run/user/1000"

[accounts.example-imap]
# sock-file = "/run/user/1000/sirup/imap.sock"

url = "imaps://example.com:993"
# tls.provider = "rustls" | "native-tls"
# tls.rustls.crypto = "ring" | "aws"
# starttls = false

sasl.plain.authcid = "example@localhost"
sasl.plain.passwd.raw = "password"

[accounts.example-smtp]
# sock-file = "/run/user/1000/sirup/smtp.sock"

url = "smtps://example.com:465"
# tls.provider = "rustls" | "native-tls"
# tls.rustls.crypto = "ring" | "aws"
# starttls = false

sasl.plain.authcid = "example@localhost"
sasl.plain.passwd.raw = "password"
