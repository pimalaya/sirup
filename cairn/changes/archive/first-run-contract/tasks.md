---
cairn: tasks
id: first-run-contract
---

- [x] Move `Cli`, `Command` and the account resolution into a cli module
- [x] Name the missing configuration, the unknown account and the missing default
- [x] Add the `configure` command and the welcome the offer prints
- [x] Save, append or print the generated account under the two save invariants
- [x] Gate every prompt on stdin, and the document on stdout
- [x] Carry `footer!` in `--help`
- [x] Shell-expand `socks-dir`, `sock-file` and `tls.cert` at deserialize
- [x] Add the `json-schema` command and the schema registry
- [x] Fold the delta into the spec and log the change
